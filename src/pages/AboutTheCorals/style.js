import styled from "styled-components";

export const StyledAbout = styled.main`
  height: auto;

  > h1 {
    font-size: 1.875rem;
    color: var(--color-primary);
    font-weight: 500;
    text-align: center;
    margin: 1.25rem 0;
  }
`;
